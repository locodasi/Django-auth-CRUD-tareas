# Django-auth-CRUD-tareas
Un CRUD de tarea hecho con Django, permite registrar un usuario y que cree sus propias tareas y completarlas.
El codigo esta configurado para su deploy en un servidor render.
